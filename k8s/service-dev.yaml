apiVersion: v1
kind: Service
metadata:
  name: csct-service
spec:
  type: NodePort
  selector:
    app: csct-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30500
